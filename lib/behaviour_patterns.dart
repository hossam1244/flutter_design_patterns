import 'package:flutter/material.dart';

class BehaviourPatterns extends StatelessWidget {
  const BehaviourPatterns({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
